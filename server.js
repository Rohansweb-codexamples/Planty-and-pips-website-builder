const express=require('express');const cors=require('cors');const app=express();app.use(cors());app.use(express.json());app.get('/',(q,s)=>s.send('Planty & Pips Server'));app.listen(process.env.PORT||3000);